# Frontend Angular

Interface web do Industrial Production Optimizer, desenvolvida com Angular.

```bash
npm install
npm start
```

A aplicação ficará disponível em `http://localhost:4200` e consumirá a API em `http://localhost:8080/api`.
